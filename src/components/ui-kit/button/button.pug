mixin Button(props = {})
  - const classes = `button ${props.className || ''}`.trim()
  button(
    class=classes
    style=props.style
    onclick!=props.onClick
  )=props.label
    if block
      block